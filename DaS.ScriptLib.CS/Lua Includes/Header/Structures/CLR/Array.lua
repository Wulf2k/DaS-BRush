---
--An array created externally, in a CLR language, such as C# or VB.net.
--Can be accessed via square brackets, just like the equivalent C# syntax (with the index starting at 0)
--@type clr.Array
clr_Array = {
    Length = 0,
}